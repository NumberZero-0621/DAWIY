# DAWIY - グローバルBPMオートメーション開発進捗まとめ

このドキュメントは、現在進行中の「グローバルBPMオートメーション」機能および関連するテンポ・マッピング機能について、何を目的として開発しているか、現状どこまで完了しているか、今後どのような課題があるかをまとめたものです。

## 1. 開発の目的と概要
DAWIYにおいて、楽曲全体のテンポ（BPM）を動的に変化させる**BPMオートメーション機能**を実装しています。
また、テンポが変更された際にオーディオリージョンやMIDIリージョンが視覚的・音楽的にどのように振る舞うべきかを再設計し、一般的なDAWにおける「ミュージカル・モード（テンポに追従する動作）」に近い体験を提供することを目指しています。

## 2. これまでに完了している実装（現状できていること）

### A. BPMオートメーションレーンのUIと描画不整合の解消
- **グローバルレーンの分離**: BPMオートメーション用のレーンを、通常のトラックとは異なる特殊なID（`trackId: -1`）として最上部に独立して描画する機構を実装しました。
- **多重生成バグの修正**: アイコンのトグル操作によりBPMレーンが無限に増殖してしまう問題を解消し、一度だけ正しく開閉されるようにライフサイクルを整備しました。
- **表示ズレの修正**: 右側のエディタキャンバスの高さ計算（`resizeCanvas`）において、ダミートラックが誤って通常トラックの高さ（`HEIGHT_TRACK`）を消費してしまう問題を修正し、他のトラックが下に隠れたりめり込んだりする表示バグを解消しました。

### B. テンポマップ (TempoMap) の導入と動的スケール
- **非線形テンポ計算**: `TempoMap` クラスを導入し、BPMがオートメーションによって曲線的に変化する場合でも、指定した絶対時間（ミリ秒）が画面上の何ピクセル目（X座標）に該当するかを積分計算で求める処理を実装しました。
- **逆関数（X座標から時間への変換）**: 波形を描画する際にピクセルから時間を割り出すため、バイナリサーチを用いた逆関数（`xToMs`）を実装しました。これにより、波形のピクセル単位でのグラデーション的な伸縮表示が可能になりました。

### C. UIテキストボックスでのBPM変更と波形伸縮の連携
- **残留データのクリーンアップ**: オートメーションレーンを閉じた後などにBPMテキストボックスを手動で変更した際、残留していた120BPMのオートメーションポイントが悪さをして「波形のガワだけが変わって中身が伸縮しない（切り取られる）」バグが発生していましたが、手動入力時にデータをリセットすることで解消しました。
- **Undo/Redoのグループ化対応**: テキストボックスへのBPM入力時にブラウザの標準Undoが働き、1文字ずつ消えてしまう問題を `e.preventDefault()` でブロックしました。同時に、DAWIYネイティブの `undoManager` に一連のテンポ変更を単一のアクションとして登録し、Ctrl+Zで一発で元のBPMと波形サイズに戻るように改善しました。

---

## 3. 今後すべきこと（残課題・ネクストアクション）

### A. MIDIリージョンの動的ストレッチ対応
- **現状**: BPMをテキストボックスで変更した際、MIDIリージョンは単純な比率（`ratio`）で `stretch(ratio)` されていますが、BPMが曲の途中で**曲線的に**変化する（オートメーションが描かれている）場合、単純な比率掛けではタイミングがずれる可能性があります。
- **対策**: BPMオートメーション下でのMIDIノートの再生タイミングや描画が、`TempoMap` を参照して動的に変化するように描画エンジンおよび再生エンジン（シーケンサー）を調整する必要があります。

### B. プレイバック（再生時）のテンポ同期の完全化
- オーディオエンジン・メトロノーム側（`HostController` および `WebAudioModule` 側）が、再生中にBPMオートメーションのカーブをリアルタイムに読み取り、スムーズにテンポを可変させているかの厳密なテストとデバッグが必要です。
- 再生中にオートメーションのポイントを跨いだ際に、プレイヘッドの移動速度が適切に加減速するかどうか（UI側の追従）を確認します。

### C. オートメーションポイント操作時の波形リアルタイム追従
- 現状、BPMオートメーションのポイントをマウスでドラッグして動かしている最中に、直下のオーディオリージョンの伸縮（再描画）がリアルタイムに反映されるか確認が必要です。
- もしドラッグ中やマウスアップ時に波形が再計算されない場合、`AutomationController` 側から関連するリージョン群への再描画（`redraw`）トリガーを発火させるロジックを追加する必要があります。

### D. UI/UXの微調整
- BPM入力テキストボックスのフォーカス時と、DAWの全体ショートカット（スペースキーでの再生など）の競合がないか確認。
- オートメーションカーブの最小値・最大値（現在の仕様では 60 〜 200 ）のレンジがユーザーにとって使いやすいものになっているかの再検討と、UI上からの上限・下限スケーリング機能の実装。
